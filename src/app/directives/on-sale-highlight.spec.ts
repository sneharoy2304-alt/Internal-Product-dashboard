import { OnSaleHighlightDirective } from './on-sale-highlight';

describe('OnSaleHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new OnSaleHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
